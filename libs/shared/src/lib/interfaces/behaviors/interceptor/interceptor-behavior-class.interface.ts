import { BehaviorClassContract } from '../behavior/behavior-class.interface';

/** Static-side contract for interceptor behavior classes. */
// eslint-disable-next-line
export interface InterceptorBehaviorClassContract<
  T
> extends BehaviorClassContract<T> {}
