/** Extensible interface for FeatureCell fluent API methods added by behaviors. */
// eslint-disable-next-line
export interface FeatureCellFluentApi<TEntity> {
  // Initially empty. Extensions will fill this in.
}
