/**
 * sessionStorage key used to persist the tab identifier across page reloads.
 * The value is a UUID generated once per browser tab session.
 */
export const TAB_SYNC_SESSION_KEY = 'sdux-vault:tab-id';
