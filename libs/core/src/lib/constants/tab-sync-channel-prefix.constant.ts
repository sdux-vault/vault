/**
 * Channel name prefix used for BroadcastChannel instances.
 * All cross-tab synchronization channels use this prefix followed by
 * the FeatureCell key to create unique, scoped channel names.
 */
export const TAB_SYNC_CHANNEL_PREFIX = 'sdux-vault:tab-sync';
