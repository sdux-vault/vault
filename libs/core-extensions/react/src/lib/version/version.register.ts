import { registerVersion } from '@sdux-vault/shared';

/** Package name used for React core-extensions version registration. */
const SDUX_PACKAGE = '@sdux-vault/react';
/** Current version of the React core-extensions package. */
const SDUX_VERSION = '1.0.3';

registerVersion(SDUX_PACKAGE, SDUX_VERSION);
