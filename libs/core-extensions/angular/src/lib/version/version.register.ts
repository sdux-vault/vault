import { registerVersion } from '@sdux-vault/shared';

/** Package name used for Angular core-extensions version registration. */
const SDUX_PACKAGE = '@sdux-vault/angular';
/** Current version of the Angular core-extensions package. */
const SDUX_VERSION = '1.0.5';

registerVersion(SDUX_PACKAGE, SDUX_VERSION);
