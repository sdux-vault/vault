/** Configuration options for the withDelay controller. */
export interface WithDelayControllerOptions {
  /** Delay in milliseconds before allowing a pipeline trace to proceed. */
  millisecondDelay: number;
}
