/** Configuration options for the withThrottle controller. */
export interface WithThrottleControllerOptions {
  /** Minimum interval in milliseconds between pipeline executions. */
  millisecondThrottle: number;
}
