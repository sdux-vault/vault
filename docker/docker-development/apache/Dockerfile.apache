FROM php:8.2-apache

# Enable required modules (proxy_wstunnel needed for Angular HMR WebSocket)
RUN a2enmod proxy proxy_http proxy_wstunnel rewrite

# Copy custom Apache config
COPY httpd.conf /etc/apache2/sites-enabled/000-default.conf
