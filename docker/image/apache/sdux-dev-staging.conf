<VirtualHost *:80>
    ServerName staging.sdux-vault.dev

    RewriteEngine On
    RewriteRule ^/?$ https://staging.sdux-vault.dev/developer [R=302,L]
    RewriteRule ^/(.*)$ https://staging.sdux-vault.dev/$1 [R=permanent,L]
</VirtualHost>

<VirtualHost *:443>
    ServerName staging.sdux-vault.dev
    DocumentRoot "/opt/bitnami/apache/htdocs"

    SSLEngine on
    SSLCertificateFile "/opt/bitnami/apache/conf/sdux-vault.com.crt"
    SSLCertificateKeyFile "/opt/bitnami/apache/conf/sdux-vault.com.key"

    # BEGIN: Redirect docs.staging.* subdomain to /docs
    RewriteEngine On
    RewriteCond %{HTTP_HOST} ^docs\. [NC]
    RewriteRule ^(.*)$ https://staging.sdux-vault.dev/docs$1 [R=permanent,L]
    # END: Redirect docs.staging.* subdomain to /docs

    # BEGIN: Route the site homepage to the developer-focused entry page
    RewriteRule ^/?$ https://staging.sdux-vault.dev/developer [R=302,L]
    # END: Route the site homepage to the developer-focused entry page

    <Directory "/opt/bitnami/apache/htdocs">
        Options Indexes FollowSymLinks
        AllowOverride All
        Require all granted
    </Directory>

    ProxyPreserveHost On
    RequestHeader set Host "sdux-vault.dev"

    ProxyPass / http://127.0.0.1:4101/
    ProxyPassReverse / http://127.0.0.1:4101/

    ErrorLog  "/opt/bitnami/apache/logs/sdux-vault-dev-staging-error.log"
    CustomLog "/opt/bitnami/apache/logs/sdux-vault-dev-staging-access.log" combined
</VirtualHost>
