<div>
  <div>
    @if (state.isLoading()) {
      <div>Loading...</div>
    } @else if (state.error()) {
      <div>{{ state.error() }}</div>
    } @else {
      <textarea readonly>{{ state.value() | json }}</textarea>
    }
  </div>

  <div>
    <button type="button" (click)="loadSample()">
      Load Sample State
    </button>

    <button type="button" (click)="loadSampleAsync()">
      Load Async State
    </button>

    <button type="button" (click)="resetState()">
      Reset State
    </button>
  </div>
</div>