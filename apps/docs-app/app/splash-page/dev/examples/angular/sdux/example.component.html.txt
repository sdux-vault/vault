<div class="example-container">
  <div class="header">
    <div class="title">Angular - SDuX Vault Initial State Example</div>
    <div class="subtitle">
      The same example is configured with the Vault runtime, one FeatureCell,
      and one service class that owns initialization and state updates.
    </div>
  </div>

  <div class="section">
    <div class="label">SDuX Flow</div>
    <div class="flow-hint">Component → FeatureCell Service → Vault → UI</div>
  </div>

  <div class="section column">
    <div class="state-container">
      <div class="label">Input State</div>
      <div class="hint">Raw data before replaceState()</div>
      <div class="hint file">
        <span class="emphasis">File:</span> app/example.component.ts
      </div>
      <textarea class="data-textarea">{{ sample | json }}</textarea>
    </div>

    <div class="state-container data-row">
      <div class="label">FeatureCell State</div>
      <div class="hint">Current Vault state snapshot</div>
      <div class="hint file">
        <span class="emphasis">File:</span> app/employee.cell.ts
      </div>
      @if (state.hasValue()) {
        <textarea class="data-textarea" readonly>{{ state.value() | json }}</textarea>
      } @else {
        <textarea class="data-textarea" readonly></textarea>
      }
      <div class="hint state">
        <span class="emphasis">Loading:</span> {{ state.isLoading() }}
      </div>
      <div class="hint state">
        <span class="emphasis">Error:</span> {{ state.error() ?? 'null' }}
      </div>
    </div>
  </div>

  <div class="section">
    <div class="actions">
      <button type="button" class="sdux-button primary" (click)="loadSample()">
        Load Sample State
      </button>

      <div class="secondary-actions">
        <button type="button" class="sdux-button" (click)="resetState()">
          Reset State
        </button>
      </div>
    </div>
  </div>
</div>