/**
 * Defines language metadata used to identify a framework-specific StackBlitz example.
 */
export interface StackBlitzExampleLanguageShape {
  /**
   * Provides the display name for the example language.
   */
  name: string;

  /**
   * Provides the identifier used to select the example language.
   */
  key: string;
}
